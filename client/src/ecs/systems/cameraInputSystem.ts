/**
 * System #1: CameraInputSystem
 *
 * Reads input state (mouse, keyboard, touch) and updates the camera
 * entity's Position/Rotation/Velocity components.
 *
 * Frequency: every frame
 */

import type { World } from 'bitecs';

export function cameraInputSystem(_world: World, _delta: number): void {
  // Stub: will read InputState and write to camera Velocity/Rotation.
  // Implementation in Phase 2 (system migration).
}
